# Log level
logLevel: info

# HLS settings (how the dashboard plays video)
hlsVariant: lowLatency
hlsSegmentCount: 7
hlsSegmentDuration: 1s
hlsAllowOrigin: '*'

# Paths — each drone stream gets auto-created
paths:
  all_others: